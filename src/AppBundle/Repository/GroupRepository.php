<?php
namespace AppBundle\Repository;

use Doctrine\ORM\EntityRepository;

/**
 * Class GroupRepository
 *
 * This class is empty because we don't need it for this particular task, but it's good
 * practice to define custom entity repositories in case the application grows
 */
class GroupRepository extends EntityRepository implements GroupRepositoryInterface
{
}
